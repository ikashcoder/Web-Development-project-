<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
</head>
<body>
  <h1><%=title %></h1>

  <h3>Current seconds :<%= seconds %></h3>
  <% if (seconds% 2 == 0) { %>

    <ul>
      <% for( let index = 0; index < items.length; index++ ) { %>
            <li>
              <%= items[index] %>
            </li>
      <% } %>
    </ul>
  <% } else {%>
    <h3>No time to display </h3>
   <% } %>
   <%- htmlContent %>
   <%- include('footer.ejs') %>
</body>
</html>